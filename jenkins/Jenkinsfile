node {
    docker.image('maven:3.9.0').inside('-v /root/.m2:/root/.m2') {
        
        stage('Build') {
            sh 'mvn -B -DskipTests clean package'
        }

        stage('Test') {
            sh 'mvn test'

            junit 'target/surefire-reports/*.xml'
        }

        stage('Deliver') {
            sh './jenkins/scripts/deliver.sh'
        }

        stage('Manual Approval') {
            input 'Lanjutkan ke tahap Deploy?'
        }

        stage('Deploy') {
            sshagent(credentials: ['ssh-key']) {
                sh 'ssh -o StrictHostKeyChecking=no root@8.219.41.186 uptime'
                sh 'ssh -v root@8.219.41.186' 
                sh 'scp /var/jenkins_home/workspace/submission-cicd-pipeline-M_ridho_hidayat/target/my-app-1.0-SNAPSHOT.jar root@8.219.41.18:/root'

            sleep 60
        }
    }
}
